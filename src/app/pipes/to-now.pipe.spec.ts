/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ToNowPipe } from './to-now.pipe';

describe('ToNowPipe', () => {
  it('create an instance', () => {
    let pipe = new ToNowPipe();
    expect(pipe).toBeTruthy();
  });
});
